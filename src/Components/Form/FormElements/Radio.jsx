import React from "react";

export const Radio = () => {
  return (
    <>
      {/* <input
        type="text"
        className={className}
        placeholder={placeholder}
        value={inputValue}
        id={id}
      />
      <label className={className} htmlFor={htmlFor}>
        {labelValue}
      </label> */}
    </>
  );
};
